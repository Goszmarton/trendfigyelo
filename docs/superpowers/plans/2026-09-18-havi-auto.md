# Havi NLP-elemzés automatikus generálása — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** A havi NLP-elemzés minden hónap utolsó napján (esti napi gyűjtés után) automatikusan lefusson az adott hónapra, és külön kiválasztható hónapként megjelenjen a Havi fülön.

**Architecture:** Új `havi_orzo.py` (utolsó-nap + idempotencia őr, `seged.esti_nap` logikai nap) + `havi.py` generáló belépő (`havi_nlp_generalas` → `havi_nlp_index_ir`, fail-soft) + `.github/workflows/havi.yml` (a „Napi trendgyűjtés" [este] befejezésére, `ANTHROPIC_API_KEY`-vel, külön commit `docs/data/havi_nlp/**`-ra) + Infó-doksi frissítés. A frontend és a `havi_nlp.py` mag VÁLTOZATLAN.

**Tech Stack:** Python 3.12 (stdlib `calendar`/`datetime`/`json`, PyYAML a teszthez), GitHub Actions, pytest.

**Spec:** docs/superpowers/specs/2026-09-18-havi-auto-generalas-design.md

## Global Constraints

- NULLA új Python-dependency (`calendar` stdlib; PyYAML már megvan). SOROS suite (pytest + Playwright) zöld.
- NINCS argless `datetime.now()` a modul-logikában — `most` paraméter / `seged.most_utc()` a belépőn.
- `git add` NÉVRE; `ATADAS-2026-08-18.txt` SOSEM staged; push külön kapuzott kör USER-jóváhagyással.
- A workflow a meglévő `ANTHROPIC_API_KEY` secretet használja; a commit-lépés CSAK `docs/data/havi_nlp`-t érinti.
- TDD: minden új modulhoz előbb bukó teszt. Irreplaceable adat READ-ONLY.

---

### Task 1: `havi_orzo.py` — utolsó-nap + idempotencia őr

**Files:**
- Create: `trendfigyelo/havi_orzo.py`
- Test: `tests/test_havi_orzo.py`

**Interfaces:**
- Consumes: `seged.esti_nap(most) -> str` ("YYYY-MM-DD", hajnal<6:00 BP → előző nap), `seged.most_utc() -> datetime`.
- Produces: `havi_logikai_honap(most) -> str`, `utolso_nap_e(most) -> bool`, `mar_kesz(docs_data, honap, logikai_nap_iso) -> bool`, `kell_generalni(docs_data, most) -> str|None`, `main(argv=None) -> int` (CLI: kiírja a hónapot vagy üres sort).

- [ ] **Step 1: Bukó tesztek** — `tests/test_havi_orzo.py`

```python
import json
from datetime import datetime, timezone

from trendfigyelo import havi_orzo


def _ir_havi(dd, honap, keszult=None):
    d = dd / "havi_nlp"
    d.mkdir(parents=True, exist_ok=True)
    art = {"honap": honap}
    if keszult is not None:
        art["keszult"] = keszult
    (d / f"{honap}.json").write_text(json.dumps(art), encoding="utf-8")


# ── utolso_nap_e ─────────────────────────────────────────────
def test_utolso_nap_szeptember_30_igaz():
    most = datetime(2026, 9, 30, 19, 0, tzinfo=timezone.utc)   # 21:00 BP, szept. 30
    assert havi_orzo.utolso_nap_e(most) is True


def test_utolso_nap_szeptember_15_hamis():
    most = datetime(2026, 9, 15, 19, 0, tzinfo=timezone.utc)
    assert havi_orzo.utolso_nap_e(most) is False


def test_utolso_nap_februar_nem_szokoev_28_igaz():
    most = datetime(2026, 2, 28, 19, 0, tzinfo=timezone.utc)   # 2026 nem szökőév
    assert havi_orzo.utolso_nap_e(most) is True


def test_utolso_nap_februar_szokoev_29_igaz_28_hamis():
    assert havi_orzo.utolso_nap_e(datetime(2028, 2, 29, 19, 0, tzinfo=timezone.utc)) is True
    assert havi_orzo.utolso_nap_e(datetime(2028, 2, 28, 19, 0, tzinfo=timezone.utc)) is False


def test_utolso_nap_30_es_31_napos_honap():
    assert havi_orzo.utolso_nap_e(datetime(2026, 4, 30, 19, 0, tzinfo=timezone.utc)) is True
    assert havi_orzo.utolso_nap_e(datetime(2026, 1, 31, 19, 0, tzinfo=timezone.utc)) is True


def test_utolso_nap_hajnali_backup_ho_fordulo_utan_elozo_ho_utolso_napja():
    # 2026-09-30T23:30Z = budapesti 2026-10-01T01:30 (<6:00) → esti_nap = 2026-09-30 (előző este)
    most = datetime(2026, 9, 30, 23, 30, tzinfo=timezone.utc)
    assert havi_orzo.utolso_nap_e(most) is True
    assert havi_orzo.havi_logikai_honap(most) == "2026-09"


# ── havi_logikai_honap ───────────────────────────────────────
def test_logikai_honap_esti():
    assert havi_orzo.havi_logikai_honap(datetime(2026, 9, 30, 19, 0, tzinfo=timezone.utc)) == "2026-09"


# ── mar_kesz ─────────────────────────────────────────────────
def test_mar_kesz_nincs_fajl_false(tmp_path):
    assert havi_orzo.mar_kesz(tmp_path, "2026-09", "2026-09-30") is False


def test_mar_kesz_mai_keszult_true(tmp_path):
    _ir_havi(tmp_path, "2026-09", keszult="2026-09-30T21:05:00+00:00")
    assert havi_orzo.mar_kesz(tmp_path, "2026-09", "2026-09-30") is True


def test_mar_kesz_ho_kozbeni_keszult_false(tmp_path):
    _ir_havi(tmp_path, "2026-09", keszult="2026-09-17T14:58:00+00:00")   # kézzel, hó közben
    assert havi_orzo.mar_kesz(tmp_path, "2026-09", "2026-09-30") is False


def test_mar_kesz_keszult_nelkul_false(tmp_path):
    _ir_havi(tmp_path, "2026-09", keszult=None)
    assert havi_orzo.mar_kesz(tmp_path, "2026-09", "2026-09-30") is False


# ── kell_generalni ───────────────────────────────────────────
def test_kell_generalni_nem_utolso_nap_none(tmp_path):
    most = datetime(2026, 9, 15, 19, 0, tzinfo=timezone.utc)
    assert havi_orzo.kell_generalni(tmp_path, most) is None


def test_kell_generalni_utolso_nap_nincs_fajl_honap(tmp_path):
    most = datetime(2026, 9, 30, 19, 0, tzinfo=timezone.utc)
    assert havi_orzo.kell_generalni(tmp_path, most) == "2026-09"


def test_kell_generalni_utolso_nap_ho_kozbeni_fajl_ujragenerál(tmp_path):
    _ir_havi(tmp_path, "2026-09", keszult="2026-09-17T14:58:00+00:00")
    most = datetime(2026, 9, 30, 19, 0, tzinfo=timezone.utc)
    assert havi_orzo.kell_generalni(tmp_path, most) == "2026-09"


def test_kell_generalni_utolso_nap_mai_fajl_none(tmp_path):
    _ir_havi(tmp_path, "2026-09", keszult="2026-09-30T21:05:00+00:00")
    most = datetime(2026, 9, 30, 19, 0, tzinfo=timezone.utc)
    assert havi_orzo.kell_generalni(tmp_path, most) is None


# ── CLI ──────────────────────────────────────────────────────
def test_cli_kiirja_a_honapot(tmp_path, capsys, monkeypatch):
    monkeypatch.setattr(havi_orzo.seged, "most_utc",
                        lambda: datetime(2026, 9, 30, 19, 0, tzinfo=timezone.utc))
    havi_orzo.main([str(tmp_path)])
    assert capsys.readouterr().out.strip() == "2026-09"


def test_cli_ures_sor_ha_nem_kell(tmp_path, capsys, monkeypatch):
    monkeypatch.setattr(havi_orzo.seged, "most_utc",
                        lambda: datetime(2026, 9, 15, 19, 0, tzinfo=timezone.utc))
    havi_orzo.main([str(tmp_path)])
    assert capsys.readouterr().out.strip() == ""
```

- [ ] **Step 2: Futtasd — piros** (`havi_orzo` nem létezik → ImportError).

Run: `python -m pytest tests/test_havi_orzo.py -q`
Expected: FAIL (ModuleNotFoundError: trendfigyelo.havi_orzo)

- [ ] **Step 3: Implementáció** — `trendfigyelo/havi_orzo.py`

```python
"""A havi NLP-elemzés utolsó-nap + idempotencia-őre (az elemzes_orzo.py mintájára).

A havi.yml a „Napi trendgyűjtés" (este) befejezésére fut; a napi backup-cronok/dispatch
ugyanazon a napon többször is elindíthatják. Ez a modul eldönti: (1) MA a hó UTOLSÓ napja-e
a `seged.esti_nap` logikai nap szerint (a hajnali <6:00 BP backup az ELŐZŐ estére sorolódik,
így a hó-forduló utáni éjfél-utáni futás nem csúszik a következő hónapra), és (2) erre a
hónapra MA már készült-e generálás (a backup-újraindítás kihagyása). Kimenet: a generálandó
hónap („YYYY-MM") vagy None/üres sor (skip).
"""
import calendar
import json
import sys
from datetime import date
from pathlib import Path

from . import seged


def havi_logikai_honap(most):
    """A logikai hónap („YYYY-MM") az esti_nap (hajnal-korrekciós) nap szerint."""
    return seged.esti_nap(most)[:7]


def utolso_nap_e(most):
    """True, ha az esti_nap logikai nap a saját hónapjának UTOLSÓ napja."""
    d = date.fromisoformat(seged.esti_nap(most))
    return d.day == calendar.monthrange(d.year, d.month)[1]


def _keszult_datum(docs_data, honap):
    """A meglévő havi_nlp/<honap>.json 'keszult' dátum-része (YYYY-MM-DD), vagy None."""
    fajl = Path(docs_data) / "havi_nlp" / f"{honap}.json"
    try:
        art = json.loads(fajl.read_text(encoding="utf-8"))
    except (OSError, ValueError):
        return None
    k = art.get("keszult") if isinstance(art, dict) else None
    return k[:10] if isinstance(k, str) and len(k) >= 10 else None


def mar_kesz(docs_data, honap, logikai_nap_iso):
    """True, ha erre a hónapra MA (a logikai napon) már generálódott (keszult dátuma == ma).

    Hiányzó/olvashatatlan/korábbi/keszult-nélküli → False: a hó-közben kézzel generált fájl
    az utolsó napon ÚJRAGENERÁLÓDIK a teljes havi adattal; egy bukott (fail-soft, fájl-írás
    nélküli) futás után a backup újrapróbál."""
    return _keszult_datum(docs_data, honap) == logikai_nap_iso


def kell_generalni(docs_data, most):
    """A generálandó hónap („YYYY-MM"), vagy None: ha nem a hó utolsó napja, vagy MA már kész."""
    if not utolso_nap_e(most):
        return None
    logikai = seged.esti_nap(most)
    honap = logikai[:7]
    if mar_kesz(docs_data, honap, logikai):
        return None
    return honap


def main(argv=None):
    """CLI: `<docs_data>` → a generálandó hónap (pl. '2026-09'), vagy ÜRES sor (skip)."""
    argv = list(sys.argv[1:] if argv is None else argv)
    docs_data = argv[0] if argv else "docs/data"
    honap = kell_generalni(docs_data, seged.most_utc())
    print(honap or "")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 4: Futtasd — zöld.** Run: `python -m pytest tests/test_havi_orzo.py -q` → PASS.

- [ ] **Step 5: Commit**

```bash
git add trendfigyelo/havi_orzo.py tests/test_havi_orzo.py
git commit -m "feat(havi): havi_orzo — utolso-nap + idempotencia or (esti_nap logikai nap)"
```

---

### Task 2: `havi.py` — generáló belépő

**Files:**
- Create: `havi.py` (repo-gyökér)
- Test: `tests/test_havi_belepo.py`

**Interfaces:**
- Consumes: `havi_nlp.havi_nlp_generalas(docs_data, honap, keszult_iso) -> dict|None`, `havi_nlp.havi_nlp_index_ir(docs_data)`, `seged.most_utc()`.
- Produces: `havi.main(argv=None) -> int` (0 siker / 1 fail-soft None / 0 üres arg).

- [ ] **Step 1: Bukó tesztek** — `tests/test_havi_belepo.py`

```python
import havi
from trendfigyelo import havi_nlp


def test_siker_index_ir_hivodik_es_0(monkeypatch):
    hivott = {"gen": 0, "idx": 0}
    monkeypatch.setattr(havi_nlp, "havi_nlp_generalas",
                        lambda dd, h, k, kliens=None: (hivott.__setitem__("gen", 1) or {"honap": h}))
    monkeypatch.setattr(havi_nlp, "havi_nlp_index_ir",
                        lambda dd: hivott.__setitem__("idx", 1))
    rc = havi.main(["2026-09"])
    assert rc == 0
    assert hivott == {"gen": 1, "idx": 1}


def test_fail_soft_none_nincs_index_es_1(monkeypatch):
    hivott = {"idx": 0}
    monkeypatch.setattr(havi_nlp, "havi_nlp_generalas", lambda dd, h, k, kliens=None: None)
    monkeypatch.setattr(havi_nlp, "havi_nlp_index_ir", lambda dd: hivott.__setitem__("idx", 1))
    rc = havi.main(["2026-09"])
    assert rc == 1
    assert hivott["idx"] == 0


def test_ures_arg_nincs_teendo_0(monkeypatch):
    monkeypatch.setattr(havi_nlp, "havi_nlp_generalas",
                        lambda *a, **k: (_ for _ in ()).throw(AssertionError("nem hívható")))
    assert havi.main([""]) == 0
    assert havi.main([]) == 0
```

- [ ] **Step 2: Futtasd — piros** (`havi.py` nem létezik → ModuleNotFoundError: havi).

Run: `python -m pytest tests/test_havi_belepo.py -q`
Expected: FAIL

- [ ] **Step 3: Implementáció** — `havi.py`

```python
"""A havi NLP-elemzés generáló belépője (a havi.yml workflow ezt hívja): a havi_orzo által
megadott hónapra generál (fail-soft), siker esetén frissíti a hónap-indexet is. Az elemzes.py
mintája; a kulcsot a havi_nlp `_NlpKliens` az ANTHROPIC_API_KEY env-ből olvassa."""
import sys

from trendfigyelo import havi_nlp, seged


def main(argv=None):
    argv = list(sys.argv[1:] if argv is None else argv)
    if not argv or not argv[0]:
        print("Nincs megadott hónap — nincs teendő.")
        return 0
    honap = argv[0]
    docs_data = "docs/data"
    keszult_iso = seged.most_utc().isoformat()
    eredmeny = havi_nlp.havi_nlp_generalas(docs_data, honap, keszult_iso)
    if eredmeny is None:
        print(f"A havi elemzés generálása elhasalt ({honap}) — nincs index-frissítés.")
        return 1
    havi_nlp.havi_nlp_index_ir(docs_data)
    print(f"Havi elemzés kész: {honap}")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 4: Futtasd — zöld.** Run: `python -m pytest tests/test_havi_belepo.py -q` → PASS.

- [ ] **Step 5: Commit**

```bash
git add havi.py tests/test_havi_belepo.py
git commit -m "feat(havi): havi.py generalo belepo (generalas -> index_ir, fail-soft)"
```

---

### Task 3: `.github/workflows/havi.yml` + YAML-teszt

**Files:**
- Create: `.github/workflows/havi.yml`
- Test: `tests/test_havi_workflow.py`

**Interfaces:**
- Consumes: `python -m trendfigyelo.havi_orzo docs/data` (a hónap vagy üres), `python havi.py "$HONAP"`, `secrets.ANTHROPIC_API_KEY`.

- [ ] **Step 1: Bukó teszt** — `tests/test_havi_workflow.py`

```python
from pathlib import Path

import yaml


def _wf():
    p = Path(__file__).resolve().parents[1] / ".github" / "workflows" / "havi.yml"
    return yaml.safe_load(p.read_text(encoding="utf-8"))


def test_havi_workflow_letezik_es_parsol():
    assert _wf() is not None


def test_trigger_napi_trendgyujtes_es_dispatch():
    wf = _wf()
    on = wf[True] if True in wf else wf["on"]     # a PyYAML az 'on:'-t True kulcsként olvashatja
    assert "workflow_dispatch" in on
    assert on["workflow_run"]["workflows"] == ["Napi trendgyűjtés"]
    assert "completed" in on["workflow_run"]["types"]


def test_generalo_lepes_anthropic_kulccsal():
    wf = _wf()
    steps = wf["jobs"]["havi"]["steps"]
    szoveg = yaml.safe_dump(steps, allow_unicode=True)
    assert "ANTHROPIC_API_KEY" in szoveg
    assert "python -m trendfigyelo.havi_orzo" in szoveg
    assert "havi.py" in szoveg


def test_commit_csak_havi_nlp_es_rebase():
    wf = _wf()
    szoveg = yaml.safe_dump(wf["jobs"]["havi"]["steps"], allow_unicode=True)
    assert "docs/data/havi_nlp" in szoveg
    assert "git pull --rebase" in szoveg
    # NEM stage-eli a napi/trend adatot
    assert "docs/data/elemzes.json" not in szoveg
```

- [ ] **Step 2: Futtasd — piros** (nincs havi.yml → FileNotFoundError a `_wf()`-ben).

Run: `python -m pytest tests/test_havi_workflow.py -q`
Expected: FAIL

- [ ] **Step 3: Implementáció** — `.github/workflows/havi.yml`

```yaml
name: Havi NLP-elemzés

on:
  workflow_run:
    workflows: ["Napi trendgyűjtés"]   # csak az esti (teljes) gyűjtés után — a teljes havi adat
    types: [completed]
  workflow_dispatch:
    inputs:
      honap:
        description: "Kézi teszt: adott hónap (YYYY-MM) újragenerálása; üres = az őr dönt"
        required: false
        default: ""

permissions:
  contents: write

concurrency:
  group: havi-futtatas
  cancel-in-progress: false

jobs:
  havi:
    # csak a napi.yml SIKERES lefutása után (kézi indításnál a feltétel átugorható)
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (a friss, épp commitolt adat)
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Python beállítása
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Függőségek telepítése
        run: pip install -r requirements.txt

      - name: "Utolsó-nap + idempotencia-őr (a generálandó hónap, vagy üres = skip)"
        id: guard
        shell: bash
        run: |
          KEZI="${{ github.event.inputs.honap }}"
          if [ -n "$KEZI" ]; then
            echo "Kézi (workflow_dispatch) felülbírálás: $KEZI"
            echo "honap=$KEZI" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          HONAP="$(python -m trendfigyelo.havi_orzo docs/data)"
          echo "Őr szerint generálandó hónap: '$HONAP' (üres = ma nincs teendő)"
          echo "honap=$HONAP" >> "$GITHUB_OUTPUT"

      - name: Havi elemzés generálása
        if: steps.guard.outputs.honap != ''
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          set -o pipefail
          python havi.py "${{ steps.guard.outputs.honap }}" 2>&1 | tee havi.log

      - name: Változások commitolása (CSAK a havi_nlp fájlok, KÜLÖN commit)
        if: always() && github.ref == 'refs/heads/main' && steps.guard.outputs.honap != ''
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git pull --rebase --autostash || true
          git add docs/data/havi_nlp
          if git diff --staged --quiet; then
            echo "Nincs havi-elemzés-változás — nincs commit."
          else
            git commit -m "adat: havi NLP-elemzés (${{ steps.guard.outputs.honap }}, $(date -u +%Y-%m-%dT%H:%MZ))"
            git push
          fi

      - name: Artefakt (a log + a havi_nlp fájlok — mindig)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: havi-${{ github.run_id }}
          retention-days: 14
          path: |
            havi.log
            docs/data/havi_nlp/**
```

- [ ] **Step 4: Futtasd — zöld.** Run: `python -m pytest tests/test_havi_workflow.py -q` → PASS.

- [ ] **Step 5: Commit**

```bash
git add .github/workflows/havi.yml tests/test_havi_workflow.py
git commit -m "feat(havi): havi.yml workflow (Napi trendgyujtes utan, utolso napon general)"
```

---

### Task 4: Infó-doksi — „Mikor készül el?" automatikusra

**Files:**
- Modify: `docs/adatokrol.html` (a „A havi elemzés" csoport „Mikor készül el?" szekciója)

Megjegyzés: az „A havi elemzés" csoport az előző körben már bekerült (nem-commitolt working-tree módosítás ezen az ágon); ez a task CSAK a „Mikor készül el?" `<p>` szövegét pontosítja az automatikus folyamatra, és staging-eli a teljes `docs/adatokrol.html`-t.

- [ ] **Step 1: Szöveg-csere** — a jelenlegi „Mikor készül el?" bekezdés:

Régi (első mondat):
```
A havi elemzés egy <strong>teljes naptári hónapot</strong> fed le, és a <strong>hónap lezárása után</strong>, a teljes havi adat összegyűltével készül el.
```
Új:
```
A havi elemzés egy <strong>teljes naptári hónapot</strong> fed le, és <strong>automatikusan, a hó utolsó napján</strong> készül el - közvetlenül az esti napi adatgyűjtés után, amikor a teljes havi adat összegyűlt. Ha az AI-hívás bármiért nem sikerül, a következő futás <strong>újrapróbálja</strong>; sosem jelenik meg félkész havi elemzés.
```
(A bekezdés második, naptár-jelölős mondata VÁLTOZATLAN.)

- [ ] **Step 2: E2E ellenőrzés** — `e2e/adatokrol` vagy a meglévő pages-teszt. Ha van `tests/test_pages.py`-ben adatokrol-assert, add hozzá:

```python
def test_adatokrol_havi_automatikus_szoveg():
    html = (DOCS / "adatokrol.html").read_text(encoding="utf-8")     # DOCS = GYOKER/"docs" (megvan)
    assert "A havi elemzés" in html
    assert "automatikusan, a hó utolsó napján" in html
```
(A `DOCS` a test_pages.py meglévő konstansa [`DOCS = GYOKER / "docs"`], ahogy a többi `test_adatokrol_*` teszt is használja.)

- [ ] **Step 3: Futtasd** — `python -m pytest tests/test_pages.py -q` → PASS (az új assert zöld).

- [ ] **Step 4: Commit**

```bash
git add docs/adatokrol.html tests/test_pages.py
git commit -m "docs(havi): Info 'Mikor keszul el?' - automatikus (ho utolso napjan) + havi szekciok"
```

## Self-Review

- **Spec-lefedettség:** trigger (T3), őr (T1), generálás+index (T2), doksi (T4) — mind megvan; marker: nincs változás (USER-döntés, spec §7).
- **Type-konzisztencia:** `havi_orzo.kell_generalni` → str|None; `main` → int; `havi.main` → int; a workflow a `havi_orzo` stdout-ját `honap` outputba teszi, üresre skippel. Egyezik.
- **Placeholder:** nincs.
- **Kockázat:** a PyYAML az `on:` kulcsot `True`-ként olvashatja (YAML 1.1) — a T3 teszt ezt kezeli (`wf[True] if True in wf else wf["on"]`).
