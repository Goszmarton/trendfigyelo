# Task 10 — mobil-geometria + célzott a11y Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** A mobil használhatóság élesítése — a mért geometria kód-formában rögzítve (render-on-load guard, rootMargin VÁLTOZATLAN), az érintési célméretek AA-ra hozva, látható fókusz, és az intervallum ok-oszlop igazítása — az L10 explicit lezárásával.

**Architecture:** Tisztán `docs/css/app.css` módosítás + új `e2e/mobil.spec.js` őrök. NINCS `docs/js` változtatás (a `rootMargin` marad; a §1 guard mutációja ideiglenes, visszaáll). NINCS Python, NINCS adat-commit.

**Tech Stack:** CSS3 (saját, külső `@import`/`url()` TILOS), Playwright (`@playwright/test`).

## Global Constraints

- **SOROS a mérvadó:** minden futás `--workers=1`. A kör VÉGÉN várt: **pytest 237** (Python érintetlen), **Playwright 65** (62 + render-on-load + date-select-magasság + media-query-jelenlét).
- **Mutáció-kapu:** a kör végén `grep -rn "MUTÁCIÓ" docs/js docs/css e2e tests trendfigyelo` == **PONTOSAN 1** (kulcsszo.spec.js:487). A §1 guard mutációja (`rootMargin:"0px"`) IDEIGLENES, visszaállítandó; NE maradjon MUTÁCIÓ-komment.
- **`rootMargin` MARAD `"400px"`** (app.js:533). A mérés szerint nincs javítandó hiba → az emelés maszkolás lenne (L10-elv). Ezt a plan EGYETLEN taskja sem változtatja meg (a §1 mutáció ideiglenes).
- **Desktop VÁLTOZATLAN:** az érintési-célméret növelés kizárólag `@media (pointer: coarse)` alatt (érintő-eszköz). A date-`<select>` globális `min-height` az egyetlen kivétel (19px egérrel is bukó AA).
- **Formák MEGMARADNAK:** a kategória-pill (`border-radius:999px`) és a szögletes intervallum-gomb változatlan; csak méret/padding nő coarse pointeren.
- **`animation:false` KÖTELEZŐ mind a 3 Chart-configban** (app.js:497/696/737) — ez tartja tárgytalanul a reduced-motiont (design §5). Ezt a plan nem bontja meg.
- **Nyelv:** minden kód+komment MAGYARUL. `git add` NÉVVEL. Nincs adat-commit.

---

## Task 1: §1 — render-on-load regressziós GUARD (a mért geometriát őrzi)

**Files:**
- Create: `e2e/mobil.spec.js`

**Interfaces:**
- Consumes: — (önálló e2e; a mock-mintát a kulcsszo.spec.js-ből ismételjük, hogy a fájl önálló legyen)
- Produces: — (végállapot-őr)

**Jelleg:** SZÁNDÉKOS ZÖLD őr (a kód már helyesen renderel). A nem-vakságot MUTÁCIÓ igazolja: `rootMargin:"0px"` → az első kártya kiesik a zónából → a teszt bukik. Ez bizonyítja, hogy a teszt a ZÓNA-LEFEDETTSÉGET méri, nem csak a canvas létezését.

**MÉRT tény (a MOCK-oldalon mérve, diag eldobva — NEM a valós szám átültetése):** a lenti mock (2 kulcsszó / 2
domén, 1_het érvényes) TÉNYLEGES boundingBox-a 360×640-en: az első `.kulcsszo-chart` teteje **708px**, canvas
betöltéskor jelen. Ez EGYEZIK a valós oldal 708px-ével, és ez NEM véletlen/kalibráció: az első kártya FÖLÖTTI
tartalom — a sáv (5 intervallum-gomb + ok-szövegek + A1-padding), a frissesség-felirat, és az ELSŐ domen-fejléc —
STRUKTURÁLIS, független a kulcsszavak/domének SZÁMÁTÓL. A valós 13 kulcsszó / 8 domén többlete mind az első kártya
ALÁ esik, ezért a 2/2 mock ugyanoda teszi az első kártyát, mint a 13/8 valós oldal.
**A guard NEM a 708-at assertálja, hanem a canvas jelenlétét** (robusztus a kis eltérésre); a 708 azt dokumentálja,
MIÉRT diszkriminál a mutáció: `rootMargin:400` zóna 1040 ≥ 708 (fedve) vs `rootMargin:0` zóna 640 < 708 (nem fedve).
A guard helyessége azon múlik, hogy a mock első kártyája a **(640, 1040)** ablakban van — ezt a Step 3 mutáció
minden futáskor ÚJRA igazolja (ha kiesne az ablakból, a mutáció NEM buktatna → a Step 3 azonnal jelezné).

- [ ] **Step 1: Írd meg az őrt — `e2e/mobil.spec.js`**

```js
const { test, expect } = require("@playwright/test");

// Task 10 §1 — render-on-load GUARD: a mért geometriát (első kulcsszó-kártya 708px, rootMargin:400 zóna 1040)
// kód-formában rögzíti. SZÁNDÉKOS ZÖLD (a kód már renderel); a nem-vakságot a MUTÁCIÓ igazolja:
// rootMargin:"0px" → zóna-alja 640 < 708 → a canvas NEM rajzolódik → a teszt bukik (lásd a plan mutáció-lépését).
// A mock a napi valós adattól FÜGGETLEN (a kulcsszo.spec.js mintája): a kártya-offszetet a sáv szerkezete adja.

const KEZD_MS = Date.parse("2026-07-29T20:00:00Z");
const iso = (h) => new Date(KEZD_MS + h * 3600000).toISOString().replace(".000Z", "+00:00");
function pontok(n) { const p = []; for (let i = 0; i < n; i++) p.push({ idopont_utc: iso(i), ertek: 30 + (i % 20), reszleges: false }); p.push({ idopont_utc: iso(n), ertek: 0, reszleges: true }); return p; }
function ivErvenyes() { return { ervenyes: true, meredekseg_nap: 1.5, se_meredekseg: 0.4, se_masodlagos_autokorrelacio: true, irany: "novekszik", r2: 0.31, r2_masodlagos_autokorrelacio: true, ablak_kezdet_utc: iso(0), ablak_veg_utc: iso(168), pontok_hasznalt: 168, pontok_kihagyva_reszleges: 1, pontok_hianyzo: 0, illesztes_vonal: [{ idopont_utc: iso(0), ertek: 35 }, { idopont_utc: iso(167), ertek: 42 }] }; }
function ivHibas(ok) { return { ervenyes: false, ok }; }
function regSzo(domen) { return { meres_kezdete: "2026-07-30", meres_vege: null, aktiv: true, domen, tipus: "szintmero", intervallumok: { "1_het": ivErvenyes(), "2_het": ivHibas("nincs_lancolas"), "1_ho": ivHibas("nincs_lancolas"), "3_ho": ivHibas("nincs_lancolas"), "1_ev": ivHibas("nincs_lancolas") } }; }
const REG = { szamitva_utc: "2026-08-05T20:37:39+00:00", meredekseg_egyseg: "relatív pont / nap", irany_kuszob: 1.0, megjegyzes: "teszt", kulcsszavak: { "állás": regSzo("munkaeropiac"), "albérlet": regSzo("lakhatas") } };
const NYERS = { kulcsszavak: { "állás": [{ kulcsszo: "állás", ablak_kezdet_utc: iso(0), ablak_veg_utc: iso(168), pontok: pontok(168) }], "albérlet": [{ kulcsszo: "albérlet", ablak_kezdet_utc: iso(0), ablak_veg_utc: iso(168), pontok: pontok(168) }] } };

async function mock_kulcsszo(page) {
  await page.route(/kulcsszo_regresszio\.json/, (r) => r.fulfill({ contentType: "application/json", body: JSON.stringify(REG) }));
  await page.route(/kulcsszo_nyers\.json/, (r) => r.fulfill({ contentType: "application/json", body: JSON.stringify(NYERS) }));
}

// ── G1 — az első kulcsszó-kártya canvas-a JELEN van betöltéskor rövid mobil-nézeten (a rootMargin:400 lefedi) ──
test("G1. render-on-load: első kulcsszó-kártya canvas jelen 360x640-en (rootMargin:400 zóna lefedi a ~708px offszetet)", async ({ page }) => {
  await page.setViewportSize({ width: 360, height: 640 });
  await mock_kulcsszo(page);
  await page.goto("/");
  await expect(page.locator("#kulcsszo-blokk .kulcsszo-chart")).toHaveCount(2);   // a mock renderel
  await page.evaluate(() => window.scrollTo(0, 0));
  // az első kártya a foldon TÚL van (offszet > 640), de a rootMargin:400 zónán BELÜL → canvas rajzolódik load-kor
  await expect(page.locator("#kulcsszo-blokk .kulcsszo-chart").first().locator("canvas")).toHaveCount(1);
});
```

- [ ] **Step 2: Futtasd — GREEN (szándékos zöld, a kód már renderel)**

Run: `npx playwright test e2e/mobil.spec.js --workers=1 --reporter=line`
Expected: **1 passed** (G1 zöld).

- [ ] **Step 3: MUTÁCIÓ — igazold, hogy a guard a zóna-lefedettséget méri**

Ideiglenesen `docs/js/app.js:533`-ban `rootMargin: "400px"` → `rootMargin: "0px"`. Futtasd:
Run: `npx playwright test e2e/mobil.spec.js -g "G1" --workers=1 --reporter=line`
Expected: **FAIL** — a canvas count 0 (az első kártya 708px, a zóna-alja most 640 < 708 → nem fedve, nem rajzol). Ez bizonyítja a nem-vakságot.

- [ ] **Step 4: ÁLLÍTSD VISSZA a mutációt**

`docs/js/app.js:533` → `rootMargin: "400px"`. Ellenőrzés: `git diff docs/js/app.js` ÜRES. NE maradjon "MUTÁCIÓ" komment sehol. Futtasd újra a G1-et → **PASS**.

- [ ] **Step 5: Commit**

```bash
git add e2e/mobil.spec.js
git commit -m "$(cat <<'EOF'
test(phase3): Task 10 §1 — render-on-load guard (mért geometria kód-formában, szándékos zöld)

A mért geometriát rögzíti: első kulcsszó-kártya 708px 360x640-en, a
rootMargin:400 zóna (1040) lefedi → canvas load-kor renderel. SZÁNDÉKOS
ZÖLD; a mutáció (rootMargin:"0px" → zóna 640 < 708 → bukik) igazolja, hogy a
teszt a ZÓNA-LEFEDETTSÉGET méri, nem a canvas puszta létét. rootMargin
VÁLTOZATLAN. A mock a napi valós adattól független.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01WJBGrwZ6giKnbZwnHYaTLz
EOF
)"
```

---

## Task 2: §2 — érintési célméret (WCAG 2.5.8 AA) + két őr

**Files:**
- Modify: `docs/css/app.css` (date-`select` globális `min-height`; új `@media (pointer: coarse)` blokk)
- Modify: `e2e/mobil.spec.js` (G2 date-select magasság, G3 media-query jelenlét)

**Interfaces:**
- Consumes: a Task 1-ben létrehozott `e2e/mobil.spec.js` (bővítjük).
- Produces: — (önálló)

**MÉRT (verifikálva): date-`<select>` 19px → BUKIK a 24px AA-t; kategória-gomb 25px; intervallum-gomb 26,6px.**

- [ ] **Step 1: Írd meg a bukó G2 tesztet — date-select ≥24px (`e2e/mobil.spec.js`, a G1 után)**

```js
// ── G2 — a dátum-<select> érintési célmérete >= 24px (WCAG 2.5.8 AA); MÉRT ma: 19px (BUKIK) ──
test("G2. a dátum-select magassága >= 24px (WCAG 2.5.8 AA; ma 19px bukna)", async ({ page }) => {
  await page.setViewportSize({ width: 360, height: 640 });
  await page.goto("/");
  const sel = page.locator("#datum-valaszto select");
  await expect(sel).toHaveCount(1);
  const bb = await sel.boundingBox();
  expect(bb.height).toBeGreaterThanOrEqual(24);
});
```
(A `#datum-valaszto` a valós `napok/index.json`-ból töltődik — a select magassága a CSS-től függ, nem az opciók számától; nem kell külön mock. Ha az élő index üres lenne és a select nem renderelne, a `toHaveCount(1)` jelzi — de a szerver valós indexet szolgál.)

- [ ] **Step 2: Futtasd — VISELKEDÉSBELI RED**

Run: `npx playwright test e2e/mobil.spec.js -g "G2" --workers=1 --reporter=line`
Expected: **FAIL** — a select LÉTEZIK (nincs timeout), de `bb.height` ≈ 19 < 24 → `expect(...).toBeGreaterThanOrEqual(24)` bukik `Received: 19`.

- [ ] **Step 3: CSS — date-select globális `min-height` (app.css, a `.vezerlo-sav` szabályok közelébe, saját sor)**

```css
/* Task 10 §2: a dátum-<select> érintési célmérete AA-ra (19px → >=24px, kényelmes 32px); egérrel is bukó volt */
#datum-valaszto select { min-height: 32px; }
```

- [ ] **Step 4: CSS — `@media (pointer: coarse)` réteg: 44px érintőn (app.css végére)**

```css
/* Task 10 §2: érintő-eszközön minden vezérlő tap-magassága >= 44px (AAA 2.5.5 komfort). A DESKTOP VÁLTOZATLAN.
   A pill/szögletes forma megmarad — csak a magasság nő. */
@media (pointer: coarse) {
  #intervallum-vezerlo button,
  #trend-blokk .kategoria-gomb,
  #datum-valaszto select { min-height: 44px; }
}
```

- [ ] **Step 5: Futtasd a G2-t — GREEN**

Run: `npx playwright test e2e/mobil.spec.js -g "G2" --workers=1 --reporter=line`
Expected: **PASS** (a select magassága ≥ 32 ≥ 24).

- [ ] **Step 6: Írd meg a G3 media-query-jelenlét őrt (`e2e/mobil.spec.js`, a G2 után)**

```js
// ── G3 — a coarse-réteg (44px érintési célméret) nem törölhető NÉMÁN: a @media (pointer: coarse) szabály LÉTEZIK ──
test("G3. a @media (pointer: coarse) réteg jelen az app.css-ben (a 44px érintési célméret őre)", async ({ page }) => {
  const css = await (await page.request.get("/css/app.css")).text();
  expect(css).toMatch(/@media\s*\(\s*pointer:\s*coarse\s*\)/);
});
```

- [ ] **Step 7: Futtasd a G3-at + a teljes mobil.spec.js-t — GREEN**

Run: `npx playwright test e2e/mobil.spec.js --workers=1 --reporter=line`
Expected: **3 passed** (G1, G2, G3).

- [ ] **Step 8: Teljes Playwright SOROS — nincs regresszió**

Run: `npx playwright test --workers=1 --reporter=line`
Expected: **65 passed** (62 + G1 + G2 + G3). A date-select `min-height` + a coarse-blokk nem töri a meglévő szerkezeti/aria teszteket.

- [ ] **Step 9: Commit**

```bash
git add docs/css/app.css e2e/mobil.spec.js
git commit -m "$(cat <<'EOF'
feat(phase3): Task 10 §2 — érintési célméret AA (date-select 19px→>=24px) + coarse 44px

A dátum-<select> globális min-height:32px (a 19px egérrel is bukó AA 2.5.8).
@media (pointer: coarse): minden vezérlő >=44px érintőn (AAA komfort), a
DESKTOP változatlan; a pill/szögletes forma marad. Két őr: G2 (date-select
magasság >=24px, RED 19px→GREEN), G3 (a coarse-réteg jelenléte — nem törölhető
némán). A kategória-gomb desktopon SZÁNDÉKOSAN marad 25px (coarse úgyis 44px).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01WJBGrwZ6giKnbZwnHYaTLz
EOF
)"
```

---

## Task 3: §3 fókusz-láthatóság + §4 intervallum ok-oszlop (kozmetika)

**Files:**
- Modify: `docs/css/app.css` (`:focus-visible` szabály; az `#intervallum-vezerlo button` alap-szabály bővítése)

**Interfaces:**
- Consumes: — · Produces: — (végállapot)

**Jelleg:** tisztán VIZUÁLIS (L9). NINCS új teszt — a user kifejezett teszt-mélység-döntése: a `:focus-visible` és a kozmetika L9-osztály, ne erőltessünk rá törékeny assertet. Az őrök a Task 1-2 szilárd e2e-i + a G3.

- [ ] **Step 1: CSS — `:focus-visible` körvonal (app.css, saját sor)**

```css
/* Task 10 §3: látható billentyűzet-fókusz (eddig nem volt focus-CSS; az A2 explicit gomb-háttere mellett
   a natív ring inkonzisztens). :focus-visible → csak billentyűzetnél, egérkattintásnál nem villan. */
#intervallum-vezerlo button:focus-visible,
#trend-blokk .kategoria-gomb:focus-visible,
#datum-valaszto select:focus-visible { outline: 2px solid #3366cc; outline-offset: 2px; }
```

- [ ] **Step 2: CSS — intervallum-gomb egységes szélesség (app.css:33, az A2-ből örökölt `#intervallum-vezerlo button` szabály bővítése)**

MÉRT: a legszélesebb label félkövéren („1 hét") 55,5px → `min-width: 3.5rem` (56px) felülről fedi → az ok-szövegek egy oszlopban igazodnak. Egészítsd ki a MEGLÉVŐ szabályt (ne írj újat):
```css
#intervallum-vezerlo button { padding: .3rem .7rem; cursor: pointer;
  background: #fff; border: 1px solid #ccc; border-radius: 4px; color: #222;
  min-width: 3.5rem; box-sizing: border-box; white-space: nowrap; }
```

- [ ] **Step 3: Teljes Playwright SOROS — nincs regresszió**

Run: `npx playwright test --workers=1 --reporter=line`
Expected: **65 passed** (nincs új teszt; a meglévők zöldek — a min-width/focus nem töri őket).

- [ ] **Step 4: Kézi vizuális szemle (L9)**

Szerver `localhost:8000`: (1) billentyűzet-Tab → a gombokon/selecten látható kék körvonal, egérkattintásnál NEM; (2) a mobil (360px) intervallum-vezérlőben az ok-szövegek EGY oszlopban kezdődnek (a gombok egyforma szélesek); (3) coarse-emulációban (DevTools) a vezérlők ~44px magasak, desktopon változatlanok.

- [ ] **Step 5: Commit**

```bash
git add docs/css/app.css
git commit -m "$(cat <<'EOF'
feat(phase3): Task 10 §3+§4 — :focus-visible + intervallum ok-oszlop igazítás

§3: látható billentyűzet-fókusz (:focus-visible outline a gombokra/selectre;
eddig nem volt focus-CSS). §4: az intervallum-gombok egységes min-width:3.5rem
(mért: a legszélesebb „1 hét" félkövéren 55,5px) → a letiltott-ok szövegek egy
oszlopban igazodnak; white-space:nowrap a label ellen. Tisztán vizuális (L9),
nincs törékeny assert (user teszt-mélység-döntése).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01WJBGrwZ6giKnbZwnHYaTLz
EOF
)"
```

---

## A kör lezárása (a Task 3 után)

- [ ] **Mutáció-kapu:** `grep -rn "MUTÁCIÓ" docs/js docs/css e2e tests trendfigyelo` → **PONTOSAN 1**. `git diff docs/js/app.js` ÜRES (a §1 mutáció visszaállt).
- [ ] **Teljes SOROS suite:** `pytest -q` (**237**) + `npx playwright test --workers=1` (**65**).
- [ ] **git status:** csak a szándékolt kód/CSS/e2e; NINCS adatfájl-módosítás; a `rootMargin` VÁLTOZATLAN.
- [ ] **L10 LEZÁRÁSA a dokumentumokban (design §6):** az ATADAS-2026-08-11.txt-ben a §4 L10-sora és a §3 Task 10-nél a +77px-re hivatkozó „rootMargin-t a KEZDŐ OFFSZETRE kell hangolni" megjegyzés **átírandó ZÁRTRA** (a @media fix megoldotta, a mai mérés igazolta: 708 vs ≥780, küszöb ~308px vh; a +77px a trend-blokkra esett). Ez controller-lépés (az ATADAS untracked).
- [ ] **Ledger-constraint rögzítése:** `animation:false` mind a 3 Chart-configban (app.js:497/696/737) KÖTELEZŐ marad; ha valaha animációt kapcsolnak be, akkor kell `prefers-reduced-motion`.
- [ ] **Whole-round review** (opus): spec-lefedettség (§1–§4 + §5-mérés + §6-zárás), a Global Constraints betartva (rootMargin változatlan, desktop változatlan, formák megmaradtak).
- [ ] **PUSH — KÜLÖN, user-jóváhagyással:** `git fetch` → `HEAD..origin/main` → ha nem üres: `pull --rebase` + suite ÚJRA → csak zöld után push → `rev-list --left-right origin/main...HEAD` == `0 0`.

## Self-review (a terv a spec ellen)

- **Spec-lefedettség:** §1 geometria+guard → Task 1 (mért 708px, mutáció rootMargin:0). §2 touch-target → Task 2 (date-select ≥24px global + coarse 44px + G2/G3). §3 focus-visible → Task 3 Step 1. §4 min-width → Task 3 Step 2 (mért 3,5rem). §5 reduced-motion-mérés → Global Constraints + kör-zárás ledger-constraint. §6 L10-zárás → kör-zárás controller-lépés. ✓ nincs fedetlen követelmény.
- **Placeholder-szken:** nincs TBD/TODO; minden CSS/JS blokk konkrét, mért értékekkel. ✓
- **Típus-konzisztencia:** a G1 mock 708px-et ad (verifikálva); a G2 `>=24` a 19px RED-del; a min-width 3,5rem a mért 55,5px felett; a coarse-regex a `@media (pointer: coarse)` szabályra illik. A `rootMargin` sehol nem változik véglegesen. ✓
